function testingGround() {
  const hello = 'hi_there'
  console.log(hello);
  // function doAThing(a, b) {
  // 	const sum = a + b;
  // 	return sum
  // }

  // function byteCount(s) {
  // 	return encodeURI(s).split(/%..|./).length - 1;
  // }

  // const someObject = {
  // 	// hello: doAThing(1, 2)
  // 	hello: byteCount('hello')
  // }

  // const someObject = {
  // 	name: 'Tom',
  // 	occupation: 'mouse catcher',
  // 	yearsInTheGame: 12
  // }

  // let key;

  // console.log(Object.getOwnPropertyNames(someObject))
  // for (key in someObject) {
  // 	if someObject.hasOwnProperty(key) {
  // 		console.log(key)
  // 	}
  // }

  // function add(a, b) {
  // 	return a + b;
  // }

  // const someObject = {
  // 	value: 0,
  // 	increment: function (inc) {
  // 		this.value += typeof inc === 'number' ? inc : 1;
  // 	},
  // 	double: function () {

  // 		console.log('outer this: ' + this)
  // 		let that = this;

  // 		let helper = function () {
  // 			console.log('inner this: ' + this)
  // 			that.value = add(that.value, that.value);
  // 		}

  // 		helper();
  // 	},
  // 	doubleArrow: function () {

  // 		console.log('outer this: ' + this)
  // 		let that = this;

  // 		let helper = () => {
  // 			console.log('inner this: ' + this)
  // 			that.value = add(that.value, that.value);
  // 		}

  // 		helper();
  // 	}
  // }
  // console.log('no inc: ' + someObject.increment())
  // console.log('no inc: ' + someObject.value)
  // console.log('no inc: ' + someObject.doubleArrow())
  // console.log('no inc: ' + someObject.value)



}


export default {
  testingGround
};